# SSL Keys & Certs 

Self-signed Cert: `certs.sh`

CRL: `crl.sh`, `openssl.cnf`, `nginx.conf`

- Cert file ext: `.pem`, `.crt`
- Key file ext: `.key`
- `.csr`: certificate signing request
- `.crl`: certificate revocation list
